node_modules
.next
out
dist
.ontology-cache
coverage
playwright-report
submodules

# Third-party specification copies — verbatim normative text, never reformatted.
# See docs/specs/references/README.md for provenance and refresh procedure.
docs/specs/references/

# Generated lockfile — managed by pnpm, never hand-/prettier-formatted
pnpm-lock.yaml

# Generated JSON Schema artifacts — content is owned by the emitting test
# (toMatchFileSnapshot over z.toJSONSchema); prettier must not reformat them.
packages/llm/schema/

# Prebuilt WebAssembly engine — Emscripten-generated glue + binary + its
# hand-kept .d.mts. Built by native/build.mjs from the pinned RAC submodule;
# never hand-/prettier-formatted. See packages/authoring-wasm/native/BUILD.md.
packages/authoring-wasm/wasm/

# Native WASM build scratch — extracted third-party sources + generated grammars.
packages/authoring-wasm/.build/

# Prebuilt esmini scenario-player WebAssembly — Emscripten SINGLE_FILE glue with
# the .wasm inlined. Built by native/build.mjs from the pinned esmini submodule;
# never hand-/prettier-formatted. See packages/scenario-viewer-wasm/native/BUILD.md.
packages/scenario-viewer-wasm/wasm/

# Native esmini WASM build scratch — CMake/Ninja tree + patched fmt includes.
packages/scenario-viewer-wasm/.build/
