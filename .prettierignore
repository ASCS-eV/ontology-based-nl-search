node_modules
.next
out
dist
.ontology-cache
coverage
playwright-report
submodules

# Third-party specification copies — verbatim normative text, never reformatted.
# See docs/specs/references/README.md for provenance and refresh procedure.
docs/specs/references/

# Generated lockfile — managed by pnpm, never hand-/prettier-formatted
pnpm-lock.yaml

# Generated JSON Schema artifacts — content is owned by the emitting test
# (toMatchFileSnapshot over z.toJSONSchema); prettier must not reformat them.
packages/llm/schema/
