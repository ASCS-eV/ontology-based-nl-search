# @ontology-search/eslint-config

Shared ESLint **flat config** (`index.mjs`) for the workspace: TypeScript
rules, `simple-import-sort`, and the project conventions (no `console.log`,
`no-explicit-any` as a warning). Consumed by each package's `eslint.config.mjs`.
