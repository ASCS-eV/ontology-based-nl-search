{
  "$schema": "./ontology-sources.schema.json",
  "description": "Example ontology-sources.json \u2014 copy to ontology-sources.json and adjust paths. Add a second source pointing at the OMB imports/ directory to opt in to the foundation vocabularies (see apps/docs/ontology.md).",
  "sources": [
    {
      "name": "omb-artifacts",
      "path": "submodules/ontology-management-base/artifacts",
      "description": "The ENVITED-X domain artifacts: {domain}/{domain}.{owl,shacl}.ttl per subdirectory."
    }
  ]
}
